- **`kadhi reward stress` now detects structure-preserving attacks that game built-in format and substring verifiers (#814 by @kok-o in #918).** `reward_stress` previously checked four classic junk strings (`empty`, `length`, `repetition`, `sentinel`), none of which exercised the structure rewarded by reasoning scaffolds. Consequently, the built-in `format` reward was reported as "robust (not gameable)" despite awarding 1.0 to `<think></think>a`, and `accuracy` missed distractor answer-sprays. `kadhi reward stress` now adds two structure-preserving attack families (`wrapped_junk` and `answer_spray`), expands each family into multiple distinct variant attempts, determines verdicts per-family to prevent denominator dilution, tests `answer_spray` against real gold substitutions with non-collapsing distractor generation, and ensures `verifiable` math checks remain robust (exit 0).
