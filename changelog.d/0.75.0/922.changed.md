- Changed `kadhi data recipe --execute` and the Python `run_recipe` API to refuse
  silent placeholder generation by default; pass a live provider or explicitly select
  offline mode (#817 by @Amix29 in #922).
