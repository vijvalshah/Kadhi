- **A test now fails when any typer command is called as a plain function with a parameter left unfilled (#752 by @dchaudhari7177 in #757).** Called directly rather than through typer, an unfilled parameter keeps its `typer.Option`/`typer.Argument` default, a truthy `OptionInfo`. That is how `kadhi eval auto` crashed after saving its results in #752, fixed at its two call sites by #754. `tests/test_typer_defaults_do_not_leak.py` walks the whole `kadhi_cli` package, resolves each call through `from X import name` to the command it names, and matches positional arguments to parameters by declaration order, so the *next* unfilled parameter anywhere fails the suite and the report names only the parameters actually left unfilled. A synthetic command proves the reporter can still report, so the guard cannot be silenced unnoticed.
