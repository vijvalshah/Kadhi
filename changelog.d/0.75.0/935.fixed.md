- Discover installed Kadhi plugins lazily, persist explicit opt-in state across processes, and
  connect enabled hooks to training (#819 by @Amix29 in #935). Third-party entry points are now
  listed from package metadata without importing their code; `kadhi plugins enable <name>` is the
  explicit code-loading boundary. Malformed recursive state and Rich-markup resource names are
  handled safely. **BREAKING:** the non-installing `kadhi plugins install` advisory now exits 2
  instead of reporting success with exit 0.
