- **`kadhi data lint` no longer fails correct conversational DPO data on `prompt_leak`, and `length_bias` needs a real size difference (#810 by @dchaudhari7177 in #916).** A conversational `chosen` / `rejected` carries its prompt as the leading user turn, and the linter flattened every turn, so `prompt_leak` flagged every row whose prompt was 40+ characters, including both shipped DPO examples, and `length_bias` measured prompt plus answer. Both checks now read only the assistant turns. `length_bias` also requires the mean lengths to differ by at least 10% for MAJOR and 5% for MINOR, on top of Cohen's d, so a consistent one-word gap between near-constant lengths (or the zero-variance fallback) is no longer MAJOR on its own.
