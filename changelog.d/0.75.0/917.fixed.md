- **`kadhi eval quant-check` now exits with code 2 on a `MAJOR` accuracy regression verdict, rejects standalone `.gguf` paths up front (exit code 3), and requires `--allow-stub` for stub scoring (#809 by @kok-o in #917).** A model load failure on either side now reports which side failed and exits with code 1 instead of silently falling back to deterministic stubs and reporting a false `OK`. Breaking: a run that previously passed on stubs now needs `--allow-stub`. When `--allow-stub` is explicitly provided, stub outputs are marked with `"stub": true` in JSON and display visible warning banners in table/markdown output.
