- **`kadhi plan` and `kadhi apply` now refuse a config carrying an unknown key (#894 by @wangzhengzhuo05 in #909).**
  `_load_yaml_config` read the YAML with `yaml.safe_load` and handed the raw mapping to
  `build_plan`, so it never built a `KadhiConfig` and never ran `find_unknown_config_keys`:
  a typo'd key planned cleanly and was refused one command later by `kadhi train`. Both
  commands now exit 1 with the loader's message and suggestion.
