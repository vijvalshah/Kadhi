- **`kadhi draft distill` and `kadhi shrink` report a validation failure in their own rendered config as a CLI error rather than a traceback (#895 by @1cbyc in #904).** Both commands validate the YAML they generate before starting a training subprocess and sanitize the validation message for terminal output.
