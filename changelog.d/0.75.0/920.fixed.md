- **`kadhi profile` no longer reports an assumed GPU as a measured fit, and knows newer and laptop cards (#830 by @dchaudhari7177 in #920).** With no GPU detected and no `--gpu`, it printed "OK Fits in 24 GB VRAM" and a batch size as if it had measured a 24 GB card; it now says no GPU was detected, shows the would-fit result against the assumed size, and asks for `--gpu`, and `--json` and the MCP `profile` tool report `gpu_memory_source` (`flag`, `detected` or `assumed`). `--gpu` now accepts the RTX 3050, 5060 through 5080 (including Ti variants), B200 and laptop parts whose memory differs from the desktop card, as well as device names as torch reports them ("NVIDIA GeForce RTX 5070 Laptop GPU"). The speed estimate is labelled as a typical A100 figure.
