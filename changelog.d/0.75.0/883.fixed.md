- **`kadhi doctor` now handles modern NVIDIA CUDA driver headers and supported PyTorch wheel indexes (#827 by @Samearth17 in #883).** CUDA detection accepts both `CUDA Version: X.Y` and `CUDA UMD Version: X.Y` headers with flexible whitespace, and PyTorch wheel selection now uses only indexes that provide releases compatible with Kadhi's `torch>=2.6.0` floor. An unknown driver version now returns `None` instead of defaulting to `cu121`, and drivers reporting CUDA 12.1–12.3 now map to `cu118` because `cu121` is not in the supported wheel table. When no supported CUDA wheel can be selected, `kadhi doctor` falls back to `pip install "kadhi-cli[train]"` instead of generating an invalid `whl/None` index. The GPU diagnostic also distinguishes a CUDA-enabled Torch build that cannot initialise CUDA from a genuine CPU-only Torch build.
