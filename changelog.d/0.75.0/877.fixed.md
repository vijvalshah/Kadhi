- Fixed `kadhi doctor` exiting 0 after reporting a `[train]` package as INCOMPATIBLE: an installed package beyond its declared ceiling (`transformers`, `peft`, `trl`) now makes the command exit non-zero, while a missing or outdated `[train]` member stays advisory (#874 by @chiliec in #877).
