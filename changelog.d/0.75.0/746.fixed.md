- **`kadhi ship` stamps the judge's actual load numerics into the verdict and `--emit-evidence`, and refuses a replay under a different family (#746 by @jagadeepmamidi, Closes #367).** Live evaluation already reused `training.quantization` for the load (#570); the absolute leg-1 / leg-2 numbers for a 4-bit run were still recorded as if the judge's precision were implicit. `--emit-evidence` now writes a top-level `numerics` stamp (`4bit` / `8bit` / `bfloat16` / `float32`), the panel and `--output` report it, and `--config` plus `--evidence` refuse a family mismatch (`4bit` vs full precision, `4bit` vs `8bit`) the same way `config_sha` refuses a drifted recipe. Pre-#367 evidence without a stamp warns rather than failing closed. This is a correctness-of-absolute-numbers fix: paired NF4 vs bf16 comparisons that judged both arms the same way remain valid.
