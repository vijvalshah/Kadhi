- **`kadhi mcp serve`'s `data_validate` no longer reports every row valid for a format it does not recognise (#878 by @Srinivasan8888 in #882).** `validate_and_stats` enables its format check only when the requested format is in `VALID_FORMATS`, so an unrecognised value silently turned the check off and a missing one never turned it on — both answered "every row valid" for a reason unrelated to the data. `kadhi data validate` was guarded against that in #869, so the same file got two different verdicts depending on whether the CLI or the MCP tool asked. The handler now resolves `format` the way `data_doctor` beside it already did: anything outside `VALID_FORMATS` raises a tool error naming the accepted values, an omitted or `auto` format is auto-detected as the tool schema always promised, and the resolved format is returned in the report so a caller can see which one was checked. The allowlist is `VALID_FORMATS` itself rather than a copy, since a hand-written second tuple is how the two surfaces drifted apart to begin with.
