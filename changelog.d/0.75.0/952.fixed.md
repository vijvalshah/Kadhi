- **`kadhi mcp serve`'s `data_doctor` now refuses blank and unknown formats instead of treating blank as auto-detect or falling through to unrelated tokenizer/doctor errors (#889 by @Rayan-and-beyond in #952).** The handler now mirrors `data_validate`'s #882 behavior: omitted `format` still auto-detects, explicit values must come from the shared `VALID_FORMATS` allowlist, and an invalid value raises the same bounded error naming the rejected value and accepted formats. Tests pin blank and near-miss rejection, message parity, every current valid format, allowlist extension without a handler change, and the omitted-format control.
