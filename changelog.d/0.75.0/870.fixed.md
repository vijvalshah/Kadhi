- **The README translation ratchet no longer passes a re-stamp without a re-translation (#870 by @Ercaner1988, follow-up to #852).** The sync stamp proves when a translation was synced, and the structure check proved headings, code-block languages, URLs and inline code survived — but a command changed inside a fence (`kadhi train` → `kadhi trainx`) or a figure changed in prose (`119.6` → `999.9`) stayed green behind a valid stamp. Each `## ` section of a translation is now also compared with `README.md` on its code-block contents with `#` comments dropped (comments being the only part of a code block a translation may change), its relative link targets, and its numbers in prose; in-page anchors, which are translated with their headings, must resolve to a heading of the same file. Measured figures therefore keep `README.md`'s digits in every language: `README.tr.md` had 10 figures written with Turkish separators (`119,6`, `3,32 GB`), now converted. Each translation names its maintainer in the test, and a red run says whom to ping.
